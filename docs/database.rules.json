{
  "rules": {
    "messages": {
      ".read": "auth != null && auth.provider != 'anonymous'",
      ".write": "auth != null"
    },
    ".read": "auth != null && auth.provider != 'anonymous'",
    ".write": "auth != null && auth.provider != 'anonymous'"
  }
}
